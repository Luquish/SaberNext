'use client'

import { ModalButton } from '@/components/tribeca/Modal/ModalButton'

/**
 * Button component that opens the Add Gauge modal
 */
function AddGaugeModalButton() {
    return (
        <ModalButton 
            buttonLabel="Add Gauge"
        >
            test
        </ModalButton>
    )
}

export { AddGaugeModalButton }